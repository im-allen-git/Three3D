<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.three3d">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".touchv1.EspTouchApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity android:name=".IndexHtmlActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.GenGcodeActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.MyAccountActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".activity.BulidModuleActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".activity.PrinterActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".activity.PrinterStartActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".activity.ShoppingActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".touchv1.EspTouchActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".activity.Esp8266Activity"
            android:label="@string/app_name" />

        <activity
            android:name=".activity.UploadGcodeActivity"
            android:label="@string/app_name" />


        <activity
            android:name=".activity.UploadDemo"
            android:label="@string/app_name" />


    </application>

</manifest>